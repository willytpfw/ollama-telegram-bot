name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run bot    
        run: 
          node index.js
        env:
          USER: ${{ secrets.USER }}
          PASSWORD: ${{ secrets.USER }}
